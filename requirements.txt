# Claude Agent SDK oficial — usada por services/claude_agent_provider.py
# para conectar o ClaudeAgentProvider.
claude-agent-sdk>=0.2.134,<1.0.0

# python-dotenv — carrega `<raiz>/.env` automaticamente na inicialização
# (config/env_loader.py), com precedência ambiente > .env > default.
# Declarado explicitamente a partir da v1.1: já vinha instalado como
# dependência transitiva, e depender disso sem declarar é frágil. Há um
# fallback mínimo em env_loader.py caso o pacote não esteja presente, então
# o JARVIS abre mesmo sem ele.
python-dotenv>=1.0,<2.0

# PySide6 (Qt for Python) — frontend/HUD (v0.5). Inclui QtQuick/QML, Qt
# Quick Shapes/Effects, e PySide6.QtAsyncio (integração oficial Qt+asyncio,
# usada em frontend/launcher.py — nenhuma dependência extra de terceiros
# para isso). O terminal (main.py) não depende de PySide6.
PySide6>=6.9,<7.0

# Voz (v0.7 — ver services/stt_service.py, services/tts_service.py,
# frontend/README.md seção "Voz"). As três são leves (poucos MB somadas) —
# o único download "grande" é o MODELO do Vosk, que o JARVIS nunca baixa
# sozinho (ver frontend/README.md para o comando manual). Sem nenhuma delas
# instalada, o JARVIS funciona normalmente por texto (voz é opcional).

# Vosk — reconhecimento de fala (STT) 100% offline, Apache 2.0, Windows
# suportado nativamente, sem GPU. https://alphacephei.com/vosk/
vosk>=0.3.32,<0.4.0

# sounddevice — captura do microfone (bindings do PortAudio), MIT,
# traz o binário do PortAudio já embutido no wheel do Windows.
# https://python-sounddevice.readthedocs.io/
sounddevice>=0.4.6,<1.0.0

# pyttsx3 — síntese de fala (TTS) via SAPI5, já nativo do Windows (nenhum
# modelo/voz extra para baixar), MPL-2.0.
# https://github.com/nateshmbhat/pyttsx3
pyttsx3>=2.90,<3.0.0

# --- STT v1.3: faster-whisper como engine principal ---
# faster-whisper — Whisper da OpenAI reimplementado sobre CTranslate2, MIT.
# Roda 100% local em CPU, sem GPU e sem API paga. Muito melhor que o Vosk em
# português: devolve texto pontuado e capitalizado e acerta frases curtas
# (ver docs/speech-to-text.md para o benchmark que motivou a troca).
# Traz numpy, ctranslate2, tokenizers, onnxruntime e huggingface-hub junto.
# O MODELO (~141 MB no tamanho `base`) nunca é baixado sozinho — só por
# `python setup.py` ou pelo HUD. https://github.com/SYSTRAN/faster-whisper
faster-whisper>=1.0,<2.0

# qrcode — gera a matriz do QR Code da ativação do 2FA (services/totp.py).
# Puro Python, BSD, sem Pillow: usamos `get_matrix()` e o HUD desenha os
# quadrados em QML, então nenhuma imagem é gerada nem gravada em disco.
qrcode>=7.4,<9.0
